/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Removing tailwindcss and leaving an empty plugins object
    // You can add other PostCSS plugins here if needed
  },
};

export default config;
